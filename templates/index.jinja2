{% extends "base.jinja2" %}
{% block content %}
    {% set directory = data.Directory or data.Video %}
    <p>{{ data.title1 or data.title2 or data.sourceTitle }} <span class="badge badge-dark">{{ data.size }}</span></p>
    <div class="row">
        <div class="col-12">
            <ul>
                {% for d in directory %}
                    {% if not d.settings %}
                        {% set uri = d.url if d.url else url(d.key) %}
                        {% set uri_external = False %}
                        {% if d.title and d.title.startswith("http") %}
                            {% set uri = d.title %}
                            {% set uri_external = True %}
                        {% endif %}
                        <li>
                            <a href="{{ uri }}"{% if uri_external %} target="_blank"{% endif %}>
                                <div class="thumb">{% if d.thumb %}<img src="{{ url(d.thumb) }}" class="rounded-left"/>{% endif %}</div>
                                <div class="title">{{ d.title.strip() }}</div>
                                <div class="summary">{{ d.summary }}</div>
                            </a>
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}